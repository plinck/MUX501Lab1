{
  "name": "functions",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tslint --project tsconfig.json && tsc",
    "watch": "tsc --watch",
    "serve": "firebase serve --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "cd .. && npm run build && cd functions && npm run setenv && cd .. && firebase deploy",
    "deployhost": "cd .. && npm run build && cd functions && npm run setenv && cd .. && firebase deploy --except functions",
    "deployfunctions": "npm run setenv && firebase deploy --only functions",
    "setenv": "test -f .env-config.json && firebase functions:config:unset env && firebase functions:config:set env=\"$(cat .env-config.json)\"",
    "logs": "firebase functions:log"
},
  "engines": {
    "node": "10"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@types/axios": "^0.14.0",
    "axios": "^0.19.2",
    "firebase": "^7.15.1",
    "firebase-admin": "^8.10.0",
    "firebase-functions": "^3.6.1"
  },
  "devDependencies": {
    "tslint": "^5.12.0",
    "typescript": "^3.8.0",
    "firebase-functions-test": "^0.2.0"
  },
  "private": true
}
